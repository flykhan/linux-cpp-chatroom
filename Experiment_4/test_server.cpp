#include "server.h"

int main()
{
    server serv(8023, "127.0.0.1"); // 创建了一个名为serv的服务器对象，该对象的构造函数参数分别为服务器的端口号8023和IP地址"127.0.0.1"，表示要监听的服务器的地址和端口
    serv.run();                     // 调用了serv对象的run函数，开始执行服务器程序并等待客户端连接
}